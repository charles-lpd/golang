package main

func main8(){
	// 八进制转成二进制
	// 规则：将八进制数每一位，转成对应的一个三位的二进制数即可，
	// 案例：请将0237 转成二进制
	// 0237 = (7 == 111) + (3 == 011) + (2 == 10)
	// 倒过来得到 10011111 === 0237




	// 八进制转换成十进制示例
	// 规则: 从最低位开始(右边的)，将每个位上的数提取出来，乘以8的(位数-1)次方，然后求和
	// 案例，请将0123 转成 十进制的数
	// 0123 = 3 * (8^(0) == 1) + 2 * (8^(1) == 8) * 1 * (8^(2) == 8 * 8) = 83
}